# Scout Notes

Target URL:

Set this per deployment with `THREAD_SEARCH_READER_URL` or by passing a reader URL to the crawl command.

Example scout observations from the original source thread on July 8, 2026:

- Main category: `Threadmarks`, category `1`, `269` threadmarks.
- Main reader pagination: `27` pages.
- Excluded by default:
  - `Sidestory`, category `5`, `212` threadmarks.
  - `Apocrypha`, category `4`, `8` threadmarks.
- Other non-default categories:
  - `Media`, category `3`.
  - `Informational`, category `6`.
  - `Staff Post`, category `2`.

The root `/reader/` URL is the main `Threadmarks` category. Other categories use URLs like `/5/reader/`.
